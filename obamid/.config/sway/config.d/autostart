# Starting audio server
exec gentoo-pipewire-launcher

# Notifications
exec mako

# Foot server for ram reduction in terminal applications
exec foot --server

# Authentication agent for gay things - replaced by keepassXC hopefully
# exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Music Player Daemon - have right now
exec mpd

# Automatic temperature setting - needs wlsunset, not sure if I actually want to have night setting
# The answer turned out to be yes...  I do, I do very much
exec ~/.config/sway/scripts/light.sh
